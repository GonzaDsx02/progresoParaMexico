import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ppm.gps.app',
  appName: 'frontEndProgresoParaMexico',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
